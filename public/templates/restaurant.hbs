<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{siteTitle}}</title>
    <style>
        /* Configuración de estilos globales */
        body {
            background-color: {{backgroundColor}};
            color: {{textColor}};
            font-family: {{fontFamily}};
            font-size: {{fontSize}};
        }

        header h1 {
            color: {{h1Color}};
            font-size: {{h1Size}};
        }

        header p {
            color: {{textColor}};
            font-size: {{sloganFontSize}};
        }

        footer {
            background-color: {{footerBackgroundColor}};
            color: {{footerTextColor}};
        }

        nav ul {
            list-style: none;
            padding: 0;
            display: flex;
            gap: 15px;
        }

        nav ul li a {
            text-decoration: none;
            color: {{primaryColor}};
            font-weight: bold;
        }

        nav ul li a:hover {
            text-decoration: underline;
        }

        .social-links a {
            text-decoration: none;
            color: {{footerTextColor}};
            margin-right: 10px;
        }

        .social-links a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <!-- Encabezado -->
    <header>
        <h1>{{siteTitle}}</h1>
        {{#if slogan}}
        <p>{{slogan}}</p>
        {{/if}}
    </header>

    <!-- Navegación -->
    <nav>
        <ul>
            {{#each menuSections}}
            <li>
                <a href="{{this.href}}">{{this.text}}</a>
            </li>
            {{/each}}
        </ul>
    </nav>

    <!-- Contenido principal -->
    <main>
        <section>
            <h2>Bienvenidos a nuestro restaurante</h2>
            <p>Explora la mejor experiencia gastronómica con los ingredientes más frescos y un ambiente acogedor.</p>
        </section>
    </main>

    <!-- Pie de Página -->
    <footer>
        <div class="social-links">
            <h3>Síguenos en nuestras redes sociales:</h3>
            <ul>
                {{#each footerSocial}}
                <li>
                    <a href="{{this.href}}" target="_blank">{{this.label}}</a>
                </li>
                {{/each}}
            </ul>
        </div>
        <p>&copy; {{year}} {{author}}</p>
    </footer>
</body>

</html>